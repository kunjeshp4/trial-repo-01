# PDF Generator

This module demonstrates creating a simple PDF using the `itextpdf` library.

Install the dependency:

```
pip install itextpdf
```

Example usage:

```
from pdf_generator import create_pdf

create_pdf("Hello world", "hello.pdf")
```
